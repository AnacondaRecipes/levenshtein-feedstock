{% set name = "levenshtein" %}
{% set version = "0.27.3" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 1ac326b2c84215795163d8a5af471188918b8797b4953ec87aaba22c9c1f9fc0

build:
  skip: true  # [py<10]
  number: 0
  script:
    - {{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vvv

requirements:
  build:
    - python
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}
    - cmake
    - make  # [unix]
  host:
    - pip
    - python
    - scikit-build-core >=0.11
    - cython >=3.0.12,<3.1.0
    - wheel
    - setuptools
  run:
    - python
    - rapidfuzz >=3.9.0,<4.0.0

test:
  source_files:
    - tests
  imports:
    - Levenshtein
  requires:
    - python
    - pip
    - pytest
  commands:
    - pip check
    - python -c "from importlib.metadata import version; assert(version('{{ name }}')=='{{ version }}')"
    - pytest -ra -vv tests

about:
  home: https://github.com/rapidfuzz/Levenshtein
  license: GPL-2.0-or-later
  license_family: GPL
  license_file: LICENSE
  summary: Python extension for computing string edit distances and similarities.
  description: | 
    The Levenshtein Python C extension module contains functions for fast
    computation of Levenshtein distance and string similarity
  dev_url: https://github.com/rapidfuzz/Levenshtein
  doc_url: https://rapidfuzz.github.io/Levenshtein

extra:
  recipe-maintainers:
    - maxbachmann
